# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
._*
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# Node (not needed for Flutter)
node_modules/
package.json
package-lock.json

# Supabase CLI binary
supabase.exe
*.tar.gz

# Supabase temp
supabase/.temp/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Environment variables
.env
.env.*
!.env.example

# Android signing (DO NOT COMMIT)
/android/key.properties
/android/*.keystore
/android/*.jks
android/key.properties
android/keystore/
*.jks
*.keystore
UsersHp.git-credentials-imagine
*.git-credentials*

# Scratch de superpowers (subagent-driven development)
.superpowers/

# Artefactos de builds anteriores
build_old/

# Nunca versionar credenciales

# Paquetes de deploy generados
dist/

# Configuracion local de Claude Code.
#
# Contiene tokens: se encontro una clave `service_role` de Supabase ahi dentro
# durante la auditoria del 29/07/2026. Estaba sin trackear y el proyecto al que
# pertenecia ya no existe, asi que no hubo filtracion — pero estaba a un
# `git add -A` de entrar al historial, y de ahi no se saca facil.
.claude/

# Salida de compilacion. Pesa ~14 MB y se regenera con build_web.py.
build/

# Cualquier variante de archivo de entorno, no solo `.env`.
*.env
.env.*
!.env.example
