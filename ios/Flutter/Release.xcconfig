#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"
#include "Generated.xcconfig"
// Allow codesign to proceed even when macOS has applied extended attributes
// (e.g. com.apple.provenance) to files in the Desktop directory (macOS Sonoma+)
OTHER_CODE_SIGN_FLAGS = --no-strict
